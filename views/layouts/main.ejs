<!DOCTYPE html>
<html>
<head>
<title><%= typeof title !== "undefined" ? title : "Campus Assistant" %></title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

<style>
*{margin:0;padding:0;box-sizing:border-box}

body{
  font-family:'Inter',sans-serif;
  background:linear-gradient(135deg,#e0f2fe,#f8fafc);
  min-height:100vh;
  display:flex;
  flex-direction:column;
}

.container{
  max-width:1200px;
  margin:auto;
  padding:50px 20px;
  flex:1;
}

a{text-decoration:none}

/* Navbar */
.navbar{
  background:white;
  padding:15px 30px;
  display:flex;
  justify-content:space-between;
  align-items:center;
  box-shadow:0 10px 25px rgba(0,0,0,0.05);
}

.nav-links{
  display:flex;
  gap:20px;
}

.nav-links a{
  font-size:14px;
  font-weight:600;
  color:#1e293b;
}

.nav-links a:hover{
  color:#6366f1;
}

/* Footer */
.footer{
  text-align:center;
  padding:20px;
  font-size:13px;
  color:#64748b;
}

</style>
</head>

<body>

<div class="navbar">

  <div>
    <a href="/" style="font-weight:700;font-size:18px;">
      🎓 Campus Assistant
    </a>
  </div>

  <div class="nav-links">
    <a href="/notes">Notes Hub</a>
    <a href="/notes/browse">Browse</a>
    <% if(user){ %>
      <a href="/notes/dashboard">Dashboard</a>
      <a href="/upload-notes">Upload</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/auth/google">Login</a>
    <% } %>
  </div>

</div>

<div class="container">
  <%- body %>
</div>

<div class="footer">
  © <%= new Date().getFullYear() %> Campus Assistant • Built for Students 🚀
</div>

</body>
</html>